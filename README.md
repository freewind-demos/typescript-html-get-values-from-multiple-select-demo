TypeScript Html Get Values from Multiple Select Demo
====================================================

对于一个multiple的select:

- `select.value`只会返回选中的第一个value
- `mySelect.selectedOptions`可以返回所有选中的options

```
npm install
npm run demo
```
